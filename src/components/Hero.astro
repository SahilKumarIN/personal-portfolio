<section id="home" class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="hero-title">
          Hi, I'm <span class="name-highlight">Sahil Kumar</span>
        </h1>
        <h2 class="hero-subtitle">Mobile App Developer</h2>
        <p class="hero-description">
          Crafting innovative mobile experiences for iOS and Android.
          Specialized in React Native, Flutter, and native development with 5+
          years of experience building apps that users love.
        </p>
        <div class="hero-buttons">
          <a href="#projects" class="btn btn-primary">View My Work</a>
          <a href="#contact" class="btn btn-outline">Get In Touch</a>
        </div>
      </div>
      <div class="hero-visual">
        <div class="hero-card">
          <div class="phone-mockup">
            <div class="phone-screen">
              <div class="app-preview">
                <div class="app-header"></div>
                <div class="app-content">
                  <div class="app-element"></div>
                  <div class="app-element"></div>
                  <div class="app-element"></div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    padding-top: 80px; /* Add padding to account for fixed navbar */
  }

  .hero::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(
        circle at 20% 20%,
        rgba(59, 130, 246, 0.1) 0%,
        transparent 50%
      ),
      radial-gradient(
        circle at 80% 80%,
        rgba(139, 92, 246, 0.1) 0%,
        transparent 50%
      );
    z-index: -1;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    padding: 2rem 0;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    line-height: 1.2;
  }

  .name-highlight {
    background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.5rem;
    color: #64748b;
    margin-bottom: 1.5rem;
    font-weight: 500;
  }

  .hero-description {
    font-size: 1.1rem;
    color: #94a3b8;
    margin-bottom: 2rem;
    max-width: 500px;
    line-height: 1.7;
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-visual {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-card {
    background: rgba(30, 41, 59, 0.5);
    border: 1px solid rgba(71, 85, 105, 0.3);
    border-radius: 2rem;
    padding: 3rem;
    backdrop-filter: blur(10px);
    transform: rotate(-5deg);
    transition: transform 0.3s ease;
  }

  .hero-card:hover {
    transform: rotate(0deg) scale(1.05);
  }

  .phone-mockup {
    width: 200px;
    height: 400px;
    background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
    border-radius: 2rem;
    padding: 1rem;
    position: relative;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
  }

  .phone-screen {
    width: 100%;
    height: 100%;
    background: #0f172a;
    border-radius: 1.5rem;
    overflow: hidden;
    position: relative;
  }

  .app-preview {
    padding: 1rem;
    height: 100%;
  }

  .app-header {
    height: 40px;
    background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
    border-radius: 0.5rem;
    margin-bottom: 1rem;
  }

  .app-content {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .app-element {
    height: 60px;
    background: rgba(71, 85, 105, 0.3);
    border-radius: 0.5rem;
    animation: pulse 2s infinite;
  }

  .app-element:nth-child(2) {
    animation-delay: 0.5s;
  }

  .app-element:nth-child(3) {
    animation-delay: 1s;
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 0.5;
    }
    50% {
      opacity: 1;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding-top: 120px; /* Increased padding for mobile to create gap */
      min-height: calc(100vh - 40px);
    }

    .hero-content {
      grid-template-columns: 1fr;
      gap: 2rem;
      text-align: center;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.3rem;
    }

    .hero-buttons {
      justify-content: center;
    }

    .phone-mockup {
      width: 150px;
      height: 300px;
    }
  }
</style>
